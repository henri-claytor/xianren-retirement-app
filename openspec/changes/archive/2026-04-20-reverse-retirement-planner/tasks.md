## 1. 頁面佈局與現況帶入

- [x] 1.1 頁面頂部新增「現況摘要」條：顯示可投資資產、月結餘（來自 `calcSummary`），若 S1 未填則顯示「—」
- [x] 1.2 移除原本「退休後每月支出」手動輸入欄，改為自動帶入（現在月支出 × 0.8），並顯示「預設為現在支出的 80%，可調整」小字提示

## 2. 情境滑桿

- [x] 2.1 新增退休年齡滑桿（min=50, max=75, step=1），預設從 store `retirementAge`，右側顯示目前值（N 歲）
- [x] 2.2 新增預期報酬率滑桿（min=2, max=12, step=0.5），預設從 store `investmentReturn`，右側顯示目前值（N%）
- [x] 2.3 滑桿樣式：`accent-blue-500`，配合 dark theme

## 3. 核心輸出卡片

- [x] 3.1 第一張（最大）StatCard：「每月需儲蓄」，顯示反推計算結果 `requiredSavings`
- [x] 3.2 第二張 StatCard：「佔月結餘比例」，顯示 `(requiredSavings / monthlySurplus * 100).toFixed(0)%`；若 monthlySurplus ≤ 0 則顯示「—（請先完成財務輸入）」
- [x] 3.3 第三張 StatCard：「目標退休金」，顯示 4% 法則計算結果（已含通膨調整）

## 4. 情境比較表

- [x] 4.1 固定顯示三列情境：目前退休年齡 -5 歲、目前、+5 歲
- [x] 4.2 每列顯示：退休年齡 / 每月需存 / 佔結餘 % 三欄
- [x] 4.3 目前選中的列高亮（`bg-blue-900/20 border-blue-800/30`），其他列用 `bg-[#202020]`

## 5. 資產成長圖表（保留並調整）

- [x] 5.1 保留現有 Recharts `ComposedChart`，X 軸為距今年數，Y 軸為萬元
- [x] 5.2 圖表線條：「現有資產成長軌跡」（依選擇的報酬率）+ 「目標退休金」水平線
- [x] 5.3 移除原本「樂觀/悲觀」雙線，改為單一情境（依滑桿報酬率）

## 6. 收尾

- [x] 6.1 移除原本「參數設定」可收合區塊（`showParams` toggle），相關參數已整合進滑桿
- [x] 6.2 保留「勞保 + 勞退」被動收入的抵扣邏輯（不改計算公式）
- [x] 6.3 確認 TypeScript 無錯誤、頁面在 S1 未填（所有數值為 0）時不顯示 NaN 或 Infinity
