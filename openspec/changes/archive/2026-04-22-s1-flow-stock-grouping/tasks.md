## 1. GroupHeader 元件

- [x] 1.1 在 S1FinancialInput.tsx 中新增 `GroupHeader` 本地元件，接受 `label` 與 `sub` props，樣式為左側粗體文字 + 右側延伸細線

## 2. 群組分隔插入

- [x] 2.1 在「月收入」區塊前插入 `<GroupHeader label="每月收支" />`
- [x] 2.2 在「現金資產」區塊前插入 `<GroupHeader label="資產負債" />`

## 3. 頁首標題更新

- [x] 3.1 將 `PageHeader title` 從「財務現況輸入」改為「我的財務」
